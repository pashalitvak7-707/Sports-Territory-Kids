<?php
/* ==========================================================================
 *  amoCRM — конфигурация (ОБРАЗЕЦ)
 *  --------------------------------------------------------------------------
 *  Скопируйте этот файл в  amo-config.php  и впишите свои значения.
 *  Файл amo-config.php НЕ попадает в git (см. .gitignore) — токен остаётся
 *  только на сервере Beget и никогда не публикуется.
 *
 *  На сервере:
 *      cp amo-config.sample.php amo-config.php
 *      nano amo-config.php      # вписать токен и секретный ключ проверки
 * ========================================================================== */

define('AMO_BASE',  'https://kidstersportru.amocrm.ru');        // адрес аккаунта
define('AMO_TOKEN', 'ВСТАВЬТЕ_СЮДА_ДОЛГОСРОЧНЫЙ_ТОКЕН');        // Bearer-токен из amoCRM

/* Секрет для страницы проверки:  amo.php?selftest=ВАШ-СЕКРЕТ
   Придумайте любую строку — она нужна, чтобы диагностику не мог открыть чужой. */
define('AMO_SELFTEST_KEY', 'смените-этот-секрет');

/* --- Необязательно ---
   Если не заданы — сделка попадёт в воронку по умолчанию, ответственный —
   по правилам amoCRM. Раскомментируйте и впишите ID, если нужно жёстко задать. */
// define('AMO_PIPELINE_ID',    0);   // ID воронки
// define('AMO_RESPONSIBLE_ID', 0);   // ID ответственного менеджера
